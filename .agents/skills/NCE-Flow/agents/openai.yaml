interface:
  display_name: "NCE Flow"
  short_description: "Repo-specific patterns and workflows for NCE-Flow"
  default_prompt: "Use the NCE-Flow repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true